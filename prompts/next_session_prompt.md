# 次セッション用 起動プロンプト（コピペして新チャットへ貼り付け）

> 下の本文をそのまま新しいチャットの最初のメッセージに貼り付けてください。
> 末尾の「今回やってほしいこと」だけ、その時の依頼に書き換えてください。

---

北名古屋・製造計画/在庫連動/日報/請求システムの開発を引き継いでほしい。実装は `app/`（Next.js 15 App Router + React 18 + TypeScript + Prisma + SQLite）。要件のソース・オブ・トゥルースはこのリポジトリの `docs/` と `source_files/`。

着手前に必ず次を読んで現状を把握して:
1. `CLAUDE.md`（常時指示。1フェーズずつ・計算式はユニットテスト・予定値と実績値を分ける 等）
2. `docs/HANDOFF_2026-06-01.md`（直近セッションで何を実装し、何が未解決かの引き継ぎ。最優先で読む）
3. `docs/00_project_overview.md`、`docs/文字起こし要約.md`、`docs/memo `（ケース表記要望）
4. Claude Code の永続メモリ（同プロジェクトなら自動ロード。索引は `MEMORY.md`：`case-count-display` / `ui-features-2026-05` / `day-staff-allocation-engine`）

現状（2026-06-01時点）の要点:
- 直近で「ケース数表記（予測/在庫/発注をケース整数・切上げ、製品＋資材対象、原料kgは対象外）」「商品検索コンボボックス」「当日割り当ての部屋ガント＋人タイムライン」「日報入力画面」「印刷のレイアウト枠隔離＋部屋ごと改ページ」を実装済み。型チェック/テスト(unit97+integration121)/本番ビルドは全グリーン。
- **重要な未解決**: 日報確定時に生産予定のPLANNED在庫が残り、`getInventoryFor`が完了プランのPLANNEDを除外しないため理論在庫が二重計上される（`docs/HANDOFF_2026-06-01.md` の §3-2 と §4-1 参照。`test/integration/pipeline_E3.test.ts` が「PLANNEDは残す」設計を固定しているので、直すなら計算層の上書きでフロー⑦と一体で）。
- 残バックログは `docs/HANDOFF_2026-06-01.md` §4（優先A: フロー⑦予実反映＋在庫二重計上、安全在庫アラート、発注書ケース化／優先B以降: 特需イベント・forecastMethod・規格合算・資材CSVケース列・監査actorId・仕入先管理UI・請求CSV列・月間印刷 等）。

進め方のルール:
- 1フェーズずつ。DB設計は将来フェーズで破綻しないよう拡張余地を持たせる。計算式はユニットテスト化。
- 予定値(`planned_*`/status=PLANNED)と実績値(`actual_*`/CONFIRMED)を混同しない。ケース換算は表示層のみ（内部数量は基本単位のまま）。
- 変更後は必ず `cd app && npx tsc --noEmit` と `npm run test` と `npx next build` を通す。gitリポジトリではないので、大きく触る前に `.snapshots/` 等へバックアップ推奨。

今回やってほしいこと:
（ここに今回の依頼を書く。例: 「フロー⑦の予実差→月間反映を、在庫二重計上の解消（計算層で完了プランのPLANNEDを除外）と一体で設計・実装してほしい。まず設計案を出してから着手して。」）
