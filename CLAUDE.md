# CLAUDE.md

このリポジトリでClaude Codeを使う場合の常時指示です。

## プロジェクト

北名古屋拠点の製造計画・在庫連動・日報・請求出力システムを実装する。ソース・オブ・トゥルースはこのZIP内の `docs/` と `source_files/` である。

旧版のLarkカレンダー要件は無視すること。今回の正しい要件は、2026-04-24の修正版文字起こしに基づく製造・在庫・日報システムである。

## 実装方針

- 初期MVPでは、生産計画の自動生成は行わない。
- 生産予定は人が登録する。
- 登録済みの生産予定から、所要時間、原料/資材使用量、在庫不足、発注候補、原価、手間賃、日報、請求用データを連動させる。
- 商品名・商品番号・原料名・資材名は正式なマスター名称を使う。曖昧な手入力名称をキーにしない。
- 部屋名や外注先名は固定値にせず、マスターで追加・変更できるようにする。
- 文字起こし上で不確実な語はコード内にハードコードしない。

## 推奨スタック

明示指定がなければ以下を推奨する。

- Frontend: Next.js + TypeScript
- Backend: Next.js API Routes または NestJS/Fastify
- DB: PostgreSQL
- ORM: Prisma
- Validation: Zod
- Test: Vitest / Playwright
- Import/Export: CSV/Excel対応

既存リポジトリのスタックがある場合は既存方針を優先する。

## 必ず守ること

- まず `docs/00_project_overview.md` と `docs/03_phase_1_manual_production_plan_mvp.md` を読んでから実装する。
- 1フェーズずつ実装する。未来フェーズを先回りして大きく作り込みすぎない。
- ただしDB設計は将来フェーズで破綻しないよう拡張余地を持たせる。
- 計算式はユニットテスト化する。
- 在庫を減らす処理、日報確定、発注確定、請求出力は監査ログを残す。
- 予定値と実績値を混同しない。`planned_*` と `actual_*` を分ける。
- 発注済みでも未確定の入荷は、確定在庫とは別扱いにする。
- 外注先/AX等の生産は、社内請求対象外になる可能性があるため、請求対象フラグを持たせる。

## 受け入れ基準

最低限、以下を通す。

- 商品マスターに1時間1人あたり生産量、レシピ/BOM、手間賃単価を設定できる。
- 生産予定に商品・数量・人数・開始時刻を入れると終了時刻が出る。
- 時間枠を指定すると、その時間内で作れる数量とあふれ数量が出る。
- 生産予定から原料/資材の予定使用量が出る。
- 在庫不足・マイナス・未確定入荷が画面で判別できる。
- 日報実績を入れると予定値ではなく実績値で在庫・原価が再計算される。
- 請求/売上伝票用CSVまたはExcelを出力できる。
